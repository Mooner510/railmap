# 시간표 CSV 리뷰 모델 분리

- 환승 추천 좌표는 null 가능 값을 명시적으로 검사한 뒤 거리 계산한다.
- 줌 기반 선형 렌더 API를 Editor와 packages/ui 사이에서 동일하게 유지한다.
- CSV 리뷰의 시간 수정, 항목 제거, 미매칭 단건/일괄 보정 로직을 별도 모델로 분리한다.
- 단일 후보 일괄 보정은 정차역 조회 Map을 한 번 구성해 대량 목록에서 반복 탐색을 줄인다.
